CEP
        复杂事件处理

      允许在无休止的事件流中监测事件模式

      目标：从简单事件中发现一些高阶特征
      输入；由一个或者多个简单事件组成
      处理：识别简单事件之间的内在联系，多个符合一定规则的简单事件构成复杂事件
      输出：满足规则的复杂事件

      PatternAPI  底层是非确定性状态机 NFA

      放

      大


      招

      严格近邻：next
      宽松近邻：followedBy


一般是C notfollowedby A next B指C和B之间不能有A